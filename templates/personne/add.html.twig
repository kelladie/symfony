{% extends 'template.html.twig' %}

{% block title %}Liste des personnes!
{% endblock %}

{% block body %}
	{{form_start(formPersonne,{'attr': {'novalidate': 'novalidate'}})}}

	<div class="row">
		<div class="col">
			{{form_row(formPersonne.firstName)}}
		</div>
		<div class="col">
			{{form_row(formPersonne.name)}}
		</div>
	</div>
	{{form_rest(formPersonne)}}
{% endblock %}
{% block javascript %}

	<script>
		$(document).ready(function () {
$('.select2').select2();

});
	</script>
{% endblock %}
